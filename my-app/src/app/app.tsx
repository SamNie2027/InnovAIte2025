import { SafeAreaView } from "react-native";
import { TattleTell } from "../components/TattleTell";

export const App = () => {
    return (
      <SafeAreaView>
          <TattleTell />
      </SafeAreaView>
    );
  };